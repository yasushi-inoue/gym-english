# Gemini API key for image generation (Phase 2: pre-generation of card scenes).
# 1. Get a key from https://aistudio.google.com/apikey  (free tier works)
# 2. Copy this file to `.env` and fill in the key below
# 3. NEVER commit `.env` — it is already in .gitignore
GEMINI_API_KEY=your_api_key_here
