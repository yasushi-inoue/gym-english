# ============================================================
# このリポジトリ固有の除外ルール
# OS自動生成ファイル・VS Code設定はグローバル側で除外:
#   C:\Users\KP000200\.gitignore_global
# ============================================================

# === 機密情報（APIキー、認証情報） ===
# Cloudflare Workers移行時に Claude API キー等を入れる可能性に備えた予防策
.env
.env.local
.env.*.local

# === Node.js（将来 npm でビルド系を導入する場合に備えて） ===
node_modules/
